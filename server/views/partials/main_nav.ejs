<!-- Navbar -->
    <nav class="navbar navbar-expand-lg sticky-top" style="background-color: #7c68ee;">
      <div class="container-fluid">
        <a class="navbar-brand mb-xlg" href="/"><h3>Nissan Skyline GTR</h3></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav ms-auto mb-2 mb-lg-0">

            <li class="nav-item">
            <% if (title == "Home") { %>
              <a class="nav-link active" style="color:white" href="/home">
            <% } else { %>
              <a class="nav-link"  style="color:black" href="/home">
            <% } %>
              <i class="fa-solid fa-lg fa-house-user"></i> Home</a>
            </li>

            <li class="nav-item">
            <% if (title == "Cars") { %>
              <a class="nav-link active" style="color:white" href="/car-info">
            <% } else { %>
              <a class="nav-link" style="color:black" href="/car-info">
            <% } %>
              <i class="fa-solid fa-lg fa-car"></i> Cars</a>
            </li>

            <li class="nav-item">
            <% if (title == "Services") { %>
              <a class="nav-link active" style="color:white" href="/services">
            <% } else { %>
              <a class="nav-link" style="color:black" href="/services">
            <% } %>
              <i class="fa-solid fa-wrench"></i> Services</a>
            </li>
  
        </div>
      </div>
    </nav>